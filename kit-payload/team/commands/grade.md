---
description: Signpost only - /grade ships in Backbrief Business OS, not in this free kit. Answers in two sentences and stops.
---

One self-check first: if THIS project's own `.claude/commands/` contains a real file of this same name (Backbrief Business OS installed per-project; real means its frontmatter does NOT begin "Signpost only" - a project file that is itself a signpost is not being shadowed), then this signpost is running from the global `~/.claude/` and is shadowing the real command - say exactly that, name the two paths, and offer to delete the global signpost so the real command can answer. Personal-level commands silently win over project-level ones; this is the one situation where this file should remove itself, with the owner's yes.

The owner typed a command that ships in Backbrief Business OS, not in this free kit. Give exactly this answer, in your own words but no longer, then stop - no added pitch, no follow-up offer, no next step. One permitted addition, because it points at FREE functionality: if what they want is a quick single-pass read on a raw idea, add that /grade-idea does that and IS in this kit.

/grade runs the full Business OS grading loop: five advisors score the business plan against the public six-dimension rubric, with cited scores and a capped revise loop. It ships in Backbrief Business OS ($79 one-time), which installs over this kit - what it adds: https://backbrief.ai/kit-vs-business-os?src=kit-stub
