---
description: Signpost only - /business-plan ships in Backbrief Business OS, not in this free kit. Answers in two sentences and stops.
---

One self-check first: if THIS project's own `.claude/commands/` contains a real file of this same name (Backbrief Business OS installed per-project; real means its frontmatter does NOT begin "Signpost only" - a project file that is itself a signpost is not being shadowed), then this signpost is running from the global `~/.claude/` and is shadowing the real command - say exactly that, name the two paths, and offer to delete the global signpost so the real command can answer. Personal-level commands silently win over project-level ones; this is the one situation where this file should remove itself, with the owner's yes.

The owner typed a command that ships in Backbrief Business OS, not in this free kit. Give exactly this answer, in your own words but no longer, then stop - no added pitch, no follow-up offer, no next step:

/business-plan is the second step of the Business OS CEO loop: it turns the business brief into a plan with 90-day execution units, ready for /grade's scoring. It ships in Backbrief Business OS ($79 one-time), which installs over this kit - what it adds: https://backbrief.ai/kit-vs-business-os?src=kit-stub
